# Entregables para la Evidencia
repos del proyecto con codigo fuente https://github.com/Alejooc/lumefy.git
## 2. Alcance aprobado para la entrega

Solo se incluyen:

- `frontend_mantis`
- `backend`

## 3. Productos a entregar

### Codigo fuente

Ya disponible en el repositorio del proyecto:

- `backend`
- `frontend_mantis`

### Archivos compilados

Frontend compilado:

- carpeta esperada: `frontend_mantis/dist`

Comando de compilacion:

```powershell
cd frontend_mantis
npm run build
```

### Documentos

Se anexan los siguientes documentos:

- `docs/ambientes_desarrollo_pruebas.md`
- `docs/diagramas_arquitectura_lumefy.md`
- `docs/entregables_evidencia_ambientes.md`

### URL

URLs de referencia del proyecto en ambiente local:

- Frontend: `http://localhost:4200`
- Backend: `http://localhost:8000`
- API Docs: `http://localhost:8000/docs`


